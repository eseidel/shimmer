name: shimmer_server
description: Server code for shimmer
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  grpc: ^3.0.2
  protobuf: ^2.1.0
  shimmer_shared:
    path: ../shimmer_shared

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.19.2
  lints: any
